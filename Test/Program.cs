using Model;
using ViewModel;

namespace Test
{
    public class Program
    {
        static void Main(string[] args)
        {
            //ExamsDB cdb = new();
            //ExamsList cList = cdb.SelectAll();
            //foreach (Exams c in cList)
            //    Console.WriteLine(c.Title);


            //Exams ExamsToUpdate = cList[0];
            //ExamsToUpdate.Title += "אאא";
            //cdb.Update(ExamsToUpdate);
            //int x = cdb.SaveChanges();
            //Console.WriteLine($"{x} rows were updated");

            //********************************************************************************************************
            //UserDB udb = new();
            //UserList uList = udb.SelectAll();
            //foreach (User u in uList)
            //    Console.WriteLine(u.Username);

            //User userToUpdate = uList[0];
            //userToUpdate.Username += "ttt";
            //udb.Update(userToUpdate);
            //int x = udb.SaveChanges();
            //Console.WriteLine($"{x} rows were updated");
            //********************************************************************************************

            //SubjectDB sudb = new();
            //SubjectList suList = sudb.SelectAll();

            //ExamsRemaindersDB cdb = new();
            //ExamsRemaindersList cList = cdb.SelectAll();
            //foreach (ExamsRemainders c in cList)
            //    Console.WriteLine(c.Subject_id.Id);

            //ExamsRemainders ExamsRemaindersToUpdate = cList[0];
            //ExamsRemaindersToUpdate.Subject_id = suList[4];
            //cdb.Update(ExamsRemaindersToUpdate);
            //int x = cdb.SaveChanges();
            //Console.WriteLine($"{x} rows were updated");
            //Console.BackgroundColor = ConsoleColor.Green;
            //*************************************************************************************************

            //ManagerDB mdb = new();
            //ManagerList mList = mdb.SelectAll();
            //foreach (Manager m in mList)
            //    Console.WriteLine(m.Id);

            //ExamsRemaindersDB edb = new();
            //ExamsRemaindersList eList = edb.SelectAll();
            //foreach (ExamsRemainders e in eList)
            //    Console.WriteLine(e.Subject_id.Subject_name);


            //PeopleDB pdb = new();
            //PeopleList pList = pdb.SelectAll();
            //foreach (People p in pList)
            //    Console.WriteLine(p.Pass);

            //People peopleToUpdate = pList[0];
            //peopleToUpdate.Pass += "אאא";
            //pdb.Update(peopleToUpdate);
            //int x = pdb.SaveChanges();
            //Console.WriteLine($"{x} rows were updated");
            // *********************************************************************************************************
            //SchedulesDB sdb = new();
            //SchedulesList sList = sdb.SelectAll();
            ////foreach (Schedules s in sList)
            ////    Console.WriteLine(s.Day_of_the_week);

            //Schedules schedulesToUpdate = sList[0];

            //schedulesToUpdate.Day_of_the_week += "aaa";
            //sdb.Update(schedulesToUpdate);
            //int x = sdb.SaveChanges();
            //Console.WriteLine($"{x} rows were updated");

            //********************************************************************************************

            //SubjectDB sudb = new();
            //SubjectList suList = sudb.SelectAll();
            //foreach (Subject su in suList)
            //    Console.WriteLine(su.Subject_name);

            //Subject SubjectToUpdate = suList[0];
            //SubjectToUpdate.Subject_name += "אאא";
            //sudb.Update(SubjectToUpdate);
            //int x = sudb.SaveChanges();
            //Console.WriteLine($"{x} rows were updated");


            //*********************************************************************************************

            //TasksRemindersDB tdb = new();
            //TasksRemindersList tList = tdb.SelectAll();
            //foreach (TasksRemainders t in tList)
            //    Console.WriteLine(t.Id);

            //ScheduleReminderDB scdb = new();
            //SchedulesRemindersList scList = scdb.SelectAll();
            //foreach (SchedulesRemainders sc in scList)
            //    Console.WriteLine(sc.Id);


            //UserSubjectDB ucdb = new();
            //UserSubjectList ucList = ucdb.SelectAll();
            //foreach (UserSubject uc in ucList)
            //    Console.WriteLine(uc.Subject_id.Id);


            //UserExamDB usdb = new();
            //UserExamList usList = usdb.SelectAll();
            //foreach (UserExam us in usList)
            //    Console.WriteLine(us.Exam_id.Exam_date);


            //ScheduleReminderDB srdb = new();
            //SchedulesRemindersList srList = srdb.SelectAll();
            //foreach (SchedulesRemainders sr in srList)
            //    Console.WriteLine(sr.SchedulesId.Hour);



        }
    }
}
