using Model;
using ViewModel;

namespace Test
{
    public class Program
    {
        static void Main(string[] args)
        {
            //ExamsDB cdb = new();
            //ExamsList cList = cdb.SelectAll();
            //foreach (Exams c in cList)
            //    Console.WriteLine(c.Title); 

            //UserDB cdb = new();
            //UserList cList = cdb.SelectAll();
            //foreach (User c in cList)
            //    Console.WriteLine(c.Username);

            //ManagerDB cdb = new();
            //ManagerList cList = cdb.SelectAll();
            // foreach (Manager c in cList)
            //     Console.WriteLine(c.Id);

            //ExamsRemaindersDB cdb = new();
            //ExamsRemaindersList cList = cdb.SelectAll();
            //foreach (ExamsRemainders c in cList)
            //    Console.WriteLine(c.Subject_id.Subject_name);


            //PeopleDB cdb = new();
            // PeopleList cList = cdb.SelectAll();
            //  foreach (People c in cList)
            //      Console.WriteLine(c.Pass);

            //SchedulesDB cdb = new();
            //SchedulesList cList = cdb.SelectAll();
            // foreach (Schedules c in cList)
            //     Console.WriteLine(c.Subject_id);           

            //SubjectDB cdb = new();
            //SubjectList cList = cdb.SelectAll();
            //foreach (Subject c in cList)
            //    Console.WriteLine(c.Subject_name);

            // TasksRemindersDB cdb = new();
            //TasksRemindersList cList = cdb.SelectAll();
            // foreach (TasksRemainders c in cList)
            //     Console.WriteLine(c.Id);

            //ScheduleReminderDB cdb = new();
            //SchedulesRemindersList cList = cdb.SelectAll();
            // foreach (SchedulesRemainders c in cList)
            //     Console.WriteLine(c.Id);                       


            //UserSubjectDB cdb = new();
            //UserSubjectList cList = cdb.SelectAll();
            //foreach (UserSubject c in cList)
            //    Console.WriteLine(c.Subject_id);


            //UserExamDB cdb = new();
            //UserExamList cList = cdb.SelectAll();
            //foreach (UserExam c in cList)
            //    Console.WriteLine(c.Exam_id.Exam_date);


          //  ScheduleReminderDB cdb = new();
          //SchedulesRemindersList cList = cdb.SelectAll();
          //  foreach (SchedulesRemainders c in cList)
          //      Console.WriteLine(c.SchedulesId);



        }
    }
}
